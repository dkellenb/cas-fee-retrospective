/* tslint:disable:no-unused-variable */

import {async, inject} from '@angular/core/testing';
import {AuthHeader} from './auth-header';

describe('AuthHeader', () => {
  it('should create an instance', () => {
    expect(new AuthHeader()).toBeTruthy();
  });
});
